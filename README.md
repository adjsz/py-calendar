# py-calendar

py-calendar is a simple CLI tool made for displaying the current date and the current calendar of the month

# Installation

Run this command in your terminal

```bash
git clone https://github.com/adjsz/py-calendar.git
cd py-calendar
python3 py-calendar
```